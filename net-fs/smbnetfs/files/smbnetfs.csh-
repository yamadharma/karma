#!/bin/csh

set MY_USER=`whoami`
set MY_PS=`ps -u $MY_USER -o comm= | grep smbnetfs$`

if ( -z "$MY_PS" && -f ~/.smbnetfs ) then
    if ( ! -d ~/net/smb ) then
	mkdir -p ~/net/smb
    endif    
    smbnetfs ~/net/smb
endif    

unset MY_USER
unset MY_PS


