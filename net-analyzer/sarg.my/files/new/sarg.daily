#!/bin/bash

# export LC_ALL=C

#Get yesterday date
YESTERDAY=$(date --date "1 day ago" +%d/%m/%Y)
 
/usr/sbin/sarg -o `webapp-config --list-installs|grep sarg`/daily -d $YESTERDAY > /dev/null 2>&1

exit 0
