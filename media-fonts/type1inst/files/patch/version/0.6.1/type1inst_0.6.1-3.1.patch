--- type1inst-0.6.1.orig/type1inst
+++ type1inst-0.6.1/type1inst
@@ -110,7 +110,8 @@
 	      "Three Islands Press:3ip",
 	      "Hank Gillette:gillette",
 	      "Doug Miles:miles",
-	      "Richard Mitchell:mitchell");
+	      "Richard Mitchell:mitchell",
+	      "Porchez Typofonderie:ptf");
 
 # Note: Hershey is the public Hershey fonts which come with Ghostscript.
 # These cause no end of problems since they look inside like funny PS
@@ -1160,10 +1161,17 @@
     }
 }
 
+sub have_pfbtops_or_die
+{
+    system "type pfbtops >/dev/null" or return;
+    die "Can't find pfbtops. Try installing groff.\n";
+}
+
 # Process binary PS fonts
 foreach $filename (do_glob("pfb")) {
     if (($dox && (! $fs{$filename})) ||
 	(($dogs || $samples) && (! $fm{$filename}))) {
+	have_pfbtops_or_die();
 	system("pfbtops $filename > foo");
 	($x, $gs) = &process_font("foo");
 	system("rm foo");
@@ -1210,6 +1218,7 @@
 foreach $filename (do_glob("pfb.gz")) {
     if (($dox && (! $fs{$filename})) ||
 	(($dogs || $samples) && (! $fm{$filename}))) {
+	have_pfbtops_or_die();
 	system("gunzip -c $filename | pfbtops > foo");
 	($x, $gs) = &process_font("foo");
 	system("rm foo");
--- type1inst-0.6.1.orig/type1inst.man
+++ type1inst-0.6.1/type1inst.man
@@ -11,7 +11,7 @@
 .if n .sp 1
 .if t .sp .5
 ..
-.TH Type1inst 0.6.1 "11-Feb-98"
+.TH Type1inst 1 "11-Feb-98"
 .SH NAME
 type1inst - Type 1 PostScript font installation utility for the X Window System and Ghostscript
 .SH VERSION
--- type1inst-0.6.1.orig/debian/README.debian
+++ type1inst-0.6.1/debian/README.debian
@@ -0,0 +1,15 @@
+type1inst for DEBIAN
+----------------------
+
+This is a useful utility to allow you to use your Adobe Type 1 fonts with
+your X server/font server and Ghostscript.
+
+The original website was at (now obsolete):
+
+  http://goblet.anu.edu.au/~m9305357/type1inst.html
+
+Cheers,
+
+ - Guenter
+
+Dr. Guenter Bechly <gbechly@debian.org>, Thu, 29 Mar 2001 08:57:32 +0200
--- type1inst-0.6.1.orig/debian/dirs
+++ type1inst-0.6.1/debian/dirs
@@ -0,0 +1,3 @@
+usr/bin
+usr/share/doc/type1inst
+usr/share/man/man1
--- type1inst-0.6.1.orig/debian/control
+++ type1inst-0.6.1/debian/control
@@ -0,0 +1,22 @@
+Source: type1inst
+Section: utils
+Priority: optional
+Maintainer: Chanop Silpa-Anan <chanop@debian.org>
+Standards-Version: 3.5.6
+Build-Depends-Indep: debhelper (>= 3.0.5)
+
+Package: type1inst
+Architecture: all
+Depends: ${perl:Depends}, groff
+Recommends: xutils
+Description: Install Adobe Type 1 fonts into X11 and Ghostscript
+ type1inst is a small perl script which generates the "fonts.scale"
+ file required by an X11 server to use any Type 1 PostScript fonts
+ which exist in a particular directory.  It gathers this information
+ from the font files themselves, a task which previously was done by
+ hand.  The script is also capable of generating the similar "Fontmap"
+ file used by ghostscript.  It can also generate sample sheets for the
+ fonts.
+ .
+ The xutils package contains a utility called makepsres, which creates
+ output in a different format but which may nevertheless be more useful.
--- type1inst-0.6.1.orig/debian/rules
+++ type1inst-0.6.1/debian/rules
@@ -0,0 +1,45 @@
+#!/usr/bin/make -f
+# Made with the aid of debmake, by Christoph Lameter,
+# based on the sample debian/rules file for GNU hello by Ian Jackson.
+
+export DH_COMPAT=3
+
+package=type1inst
+
+build:
+	dh_testdir
+	
+	touch build
+
+clean:
+	dh_testdir
+	dh_testroot
+	dh_clean build
+
+binary-indep: build
+# There are no architecture-independent files to be uploaded
+# generated by this package.  If there were any they would be
+# made here.
+
+binary-arch: build
+	dh_testdir
+	dh_testroot
+	dh_clean -k
+	dh_installdirs
+	install t1embed type1inst debian/$(package)/usr/bin
+	dh_installdocs README
+	dh_installman type1inst.man
+	dh_link usr/share/man/man1/type1inst.1.gz \
+		usr/share/man/man1/t1embed.1.gz
+	dh_installchangelogs ChangeLog
+	dh_compress
+	dh_fixperms
+	dh_installdeb
+	dh_perl
+	dh_gencontrol
+	dh_md5sums
+	dh_builddeb
+
+binary: binary-indep binary-arch
+
+.PHONY: binary binary-arch binary-indep clean
--- type1inst-0.6.1.orig/debian/changelog
+++ type1inst-0.6.1/debian/changelog
@@ -0,0 +1,64 @@
+type1inst (0.6.1-3.1) unstable; urgency=low
+
+  * NMU.
+  * Change groff to a dependency.  (Closes: #138500)
+
+ -- Chris Lawrence <lawrencc@debian.org>  Mon, 18 Mar 2002 02:24:10 -0600
+
+type1inst (0.6.1-3) unstable; urgency=low
+
+  * Patch type1inst to tell user about groff. (closes: #119614)
+    Thank you Stefan Karpinsk.
+
+ -- Chanop Silpa-Anan <chanop@debian.org>  Tue, 27 Nov 2001 09:23:49 +1100
+
+type1inst (0.6.1-2.1) unstable; urgency=low
+
+  * Minor patch from Jean-Philippe Gurard. (closes: #110140)
+  * Standards-Version 3.5.6.
+
+ -- Chanop Silpa-Anan <chanop@debian.org>  Thu, 30 Aug 2001 10:32:35 +1000
+
+type1inst (0.6.1-2) unstable; urgency=low
+
+  * New maintainer; closes: #100268
+  * Standards-Version 3.5.5. 
+
+ -- Chanop Silpa-Anan <chanop@debian.org>  Fri, 15 Jun 2001 19:55:57 +1000
+
+type1inst (0.6.1-1) unstable; urgency=low
+
+  * New maintainer; closes: #90881
+  * New upstream release; closes: #57233
+  * Corrected manpage (.TH).
+  * Updated download URL in copyright and file from sunsite to metalab.
+  * Removed watch file because program is dead upstream.
+  * Removed symlink for xgentype1dir.1 manpage, because this program was
+    removed in this new upstream version.
+  * Modified README.Debian and dirs file.
+
+ -- Dr. Guenter Bechly <gbechly@debian.org>  Thu, 29 Mar 2001 08:57:32 +0200
+
+type1inst (0.6-2) unstable; urgency=low
+
+  * The maintainer has orphaned this package (see bug #90881); set
+    maintainer to Debian QA Group.
+  * Update to policy version 3.5.2:
+    - FHS updates (closes: #91051, #91689).
+    - Build dependencies.
+  * Converted to debhelper.
+  * Fixed the section in the man page's .TH request.
+  * Fixed spelling error in package description (lintian).
+  * Updated GPL pointer in debian/copyright.
+  * Recommends: groff, for pfbtops (sometimes used by t1embed), and xutils,
+    for mkfontdir (sometimes used by type1inst).
+  * Added a note in the package description that makepsres in xutils might
+    be a better tool to use.
+
+ -- Colin Watson <cjwatson@debian.org>  Tue, 27 Mar 2001 23:20:39 +0100
+
+type1inst (0.6-1) unstable; urgency=low
+
+  * Initial Release.
+
+ -- Jim Pick <jim@jimpick.com>  Sat, 14 Mar 1998 00:04:00 -0800
--- type1inst-0.6.1.orig/debian/copyright
+++ type1inst-0.6.1/debian/copyright
@@ -0,0 +1,20 @@
+This package was debianized by Dr. Guenter Bechly <gbechly@debian.org> on
+Thu, 29 Mar 2001 08:57:32 +0200.
+
+It was downloaded from ftp://metalab.unc.edu/pub/Linux/X11/xutils/
+
+Copyright:
+
+#  Copyright (C) 1996-1998 James Macnicol
+#
+#  This program is free software; you can redistribute it and/or modify
+# it under the terms of the GNU General Public License as published by the
+# Free Software Foundation; either version 2, or (at your option) any later
+# version.
+#
+#  This program is distributed in the hope that it will be useful, but
+# WITHOUT ANY WARRANTY; without even the implied warranty of MERCHANTIBILITY
+# or FITNESS FOR A PARTICULAR PURPOSE.  See the GNU General Public License
+# for more details.
+
+The full GPL license is in /usr/share/common-licenses/GPL
