Fix paludis direct-list-initialization C++11 error.
See also:
- https://bugs.gentoo.org/show_bug.cgi?id=562212
- http://comments.gmane.org/gmane.linux.gentoo.paludis.user/2042

--- paludis-2.4.0/paludis/resolver/collect_depped_upon.cc
+++ paludis-2.4.0/paludis/resolver/collect_depped_upon.cc
@@ -239,7 +239,7 @@
     }
     else
     {
-        auto k { &PackageID::build_dependencies_key, &PackageID::run_dependencies_key, &PackageID::post_dependencies_key };
+        auto k = { &PackageID::build_dependencies_key, &PackageID::run_dependencies_key, &PackageID::post_dependencies_key };
         for (auto i(k.begin()), i_end(k.end()) ; i != i_end ; ++i)
         {
             auto key(((*id).*(*i))());
